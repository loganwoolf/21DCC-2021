//input 
const speed = 40
  const missionData = {
    astro:["...","..."], 
    bio:["..."], 
    physics:["..."]
  }

  const checks = {
    maxSpeed:50,
    minSpeed:20,
    dataEntries:{
      astro:3,
      bio:1,
      physics:1
    }
  }
   
  
//output
false // Not the same amount of entries

//original code 

const confirmReentryPlans = (speed, missionData, checks) => {
  // Code here!
}
